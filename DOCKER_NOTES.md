# Docker 部署注意事项和副作用说明

## Docker 的潜在副作用

### 1. 性能影响

**轻微的性能开销**
- Docker 容器会有约 5-10% 的性能开销（主要是网络和文件系统）
- 对于大多数 Web 应用，这个开销可以忽略不计
- 如果服务器资源充足，影响很小

**建议**：
- 如果服务器配置较低（< 2GB RAM），可以考虑手动部署
- 对于中小型应用，Docker 的性能影响通常可以接受

### 2. 资源占用

**额外的内存和磁盘占用**
- Docker 守护进程本身占用约 100-200MB 内存
- 每个容器镜像占用磁盘空间（约 500MB - 2GB）
- 容器日志可能占用磁盘空间

**建议**：
- 定期清理未使用的镜像：`docker system prune -a`
- 配置日志轮转，限制日志大小
- 监控磁盘使用情况

### 3. 网络复杂性

**容器间网络**
- Docker 网络可能增加调试复杂度
- 容器间通信需要通过 Docker 网络

**已处理**：
- 在 `docker-compose.yml` 中已配置专用网络
- 服务间通过服务名通信（如 `meilisearch:7700`）

### 4. 数据持久化

**数据卷管理**
- 需要确保数据卷正确挂载
- 备份和恢复需要额外考虑

**已处理**：
- 数据库文件通过 volume 挂载到宿主机
- MeiliSearch 数据使用 Docker volume（自动持久化）
- 头像文件通过 volume 挂载

### 5. 调试难度

**日志查看**
- 需要额外的命令查看日志：`docker compose logs`
- 进入容器调试需要额外步骤

**解决方案**：
- 使用 `docker compose logs -f` 实时查看日志
- 使用 `docker compose exec backend sh` 进入容器

### 6. 依赖更新

**系统依赖更新**
- 容器内的系统包更新需要重建镜像
- Node.js 版本更新需要修改 Dockerfile

**建议**：
- 定期更新基础镜像
- 使用固定版本的基础镜像（如 `node:20-alpine`）

### 7. 文件权限

**Linux 文件权限**
- 容器内外的用户 ID 可能不同
- 可能导致文件权限问题

**已处理**：
- 使用非 root 用户运行（Node.js 镜像默认）
- 数据目录权限已正确配置

## Docker 的优势

### 1. 环境一致性
- 开发、测试、生产环境完全一致
- 避免"在我机器上能跑"的问题

### 2. 隔离性
- 服务之间相互隔离
- 一个服务的问题不会影响其他服务

### 3. 易于部署
- 一键启动所有服务
- 无需手动安装和配置依赖

### 4. 易于扩展
- 可以轻松横向扩展服务
- 可以快速添加新服务

### 5. 版本管理
- 可以轻松回滚到之前的版本
- 可以同时运行多个版本进行测试

## 是否应该使用 Docker？

### 推荐使用 Docker 的情况：
- ✅ 需要快速部署和迁移
- ✅ 团队协作，需要环境一致性
- ✅ 需要同时运行多个服务
- ✅ 服务器资源充足（> 2GB RAM）
- ✅ 需要频繁更新和部署

### 不推荐使用 Docker 的情况：
- ❌ 服务器资源非常有限（< 1GB RAM）
- ❌ 对性能要求极高（需要极致优化）
- ❌ 团队对 Docker 不熟悉
- ❌ 只需要运行单个简单服务

## 替代方案

如果不想使用 Docker，可以参考 `DEPLOY.md` 中的"方式二：手动部署"部分。

## 常见问题

### Q: Docker 会影响数据库性能吗？
A: 对于 SQLite 这种轻量级数据库，Docker 的性能影响很小。如果使用 volume 挂载，性能几乎无影响。

### Q: 容器重启会丢失数据吗？
A: 不会。数据通过 volume 挂载到宿主机，容器重启不会影响数据。

### Q: 如何监控容器资源使用？
A: 使用 `docker stats` 命令可以实时查看容器资源使用情况。

### Q: 容器崩溃会自动重启吗？
A: 是的。`docker-compose.yml` 中配置了 `restart: unless-stopped`，容器会自动重启。

## 总结

对于您的项目（吟心），**推荐使用 Docker**，因为：
1. 项目包含多个服务（前端、后端、MeiliSearch）
2. Docker 可以简化部署流程
3. 便于后续维护和更新
4. 性能影响可以接受

如果遇到性能问题，可以考虑：
- 优化代码和数据库查询
- 增加服务器资源
- 使用更轻量的基础镜像（如 alpine）

